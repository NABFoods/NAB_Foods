import React, { FC } from 'react';
import { createRoot } from 'react-dom/client';
import Cart from './cart/Cart'

const App: FC = () => {
  return (
    <div>
      < Cart />
      {/* <h1>HELLO THERE!</h1> */}
    </div>
  );
};

export default App;
